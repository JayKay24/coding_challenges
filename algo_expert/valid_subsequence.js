const isValidSubsequence = (array, sequence) => {
  let winStart = 0, result = false;

  for (let winEnd = 0; winEnd < array.length; i++) {
    
  }
};

console.log(isValidSubsequence([5, 1, 22, 25, 6, -1, 8, 10], [1, 6, -1, 10]));